<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.workout.renedix.renedixworkout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".settings.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.workout.renedix.renedixworkout.MainActivity" />
        </activity>
        <activity
            android:name=".setup.cardio.CardioExerciseForm"
            android:label="Cardio Exercise">
        </activity>
        <activity
            android:name=".setup.resistance.ResistanceExerciseForm"
            android:label="Resistance Exercise">
        </activity>
        <activity
            android:name=".setup.workout.WorkoutForm"
            android:label="Workout">
        </activity>
        <activity android:name=".setup.workout.AddExerciseActivity">
        </activity>
    </application>

</manifest>